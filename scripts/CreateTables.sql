CREATE TABLE "EUDLI"."CUSTOMERS_MG"
(
   "CUSTOMER_ID" NUMBER(10,0) GENERATED BY DEFAULT ON NULL AS IDENTITY,
   "CUSTOMER_NAME" VARCHAR2 (50 BYTE),
   "CITY" VARCHAR2 (50 BYTE),
   primary key (CUSTOMER_ID)
);